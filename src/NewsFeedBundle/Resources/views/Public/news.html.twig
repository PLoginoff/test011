{% extends "NewsFeedBundle::base.html.twig" %}

{% block title %}NewsFeedBundle:Public:news{% endblock %}

{% block body %}
    <h1>{% trans %}news.wellcome{% endtrans %} NewsFeed</h1>
    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Img</th>
                    <th>Title</th>
                    <th>Text</th> {# FIXME обрезать. Входяие все сконвертить! #}
                    <th>User</th>
                    <th></th>
                </tr>
            </thead>
            {% for a in list %}
                <tr>
                    <td>{{a.id}}.</td>
                    <td><img class="news-img" src="{{asset('uploads/articles/' ~ a.photo)}}"/></td>
                    <td>{{a.title}}</td>
                    <td>{{a.text}}</td> {# FIXME обрезать. Входяие все сконвертить! #}
                    <td>{{a.user}}</td>
                    <td>
                        <a href="{{path('public_view', {id: a.id})}}" class="btn btn-info">View</a>
                        <a href="{{path('public_pdf',  {id: a.id})}}" class="btn btn-info">PDF</a>
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>

{% endblock %}




